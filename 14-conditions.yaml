- name: conditions
  hosts: web
  become: true
  tasks:
  - name: check user exist or not
    ansible.builtin.command: id expense
    ignore_errors: true

  - name: say Hello
    ansible.builtin.debug:
      msg: "Hi hello"